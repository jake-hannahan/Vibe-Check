const TipMessage = () => (
	<span className={`bg-gray-800 px-1 text-sm text-gray-100 rounded-md absolute right-0
					translate-x-full m-4 mx-auto`}>
		To find the Spotify playlist id enter the playlist page, click the (...) button near the play button,
		and click "Share: Copy Spotify URI". The playlist id is the string right after "spotify:playlist:".
	</span>
)

export default TipMessage;
